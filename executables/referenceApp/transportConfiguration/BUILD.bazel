# Transport configuration for the reference application.

cc_library(
    name = "transportConfiguration",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
