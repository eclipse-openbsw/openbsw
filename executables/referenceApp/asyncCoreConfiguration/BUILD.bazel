# Async core configuration for the reference application.

cc_library(
    name = "asyncCoreConfiguration",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
