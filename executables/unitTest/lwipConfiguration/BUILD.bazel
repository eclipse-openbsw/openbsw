# lwIP configuration for unit tests.

cc_library(
    name = "lwipConfiguration",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    deps = ["//platforms/posix/lwipSysArch"],
)
