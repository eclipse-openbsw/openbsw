# Transport configuration for unit tests.

# Empty transport configuration for unit tests (matches CMake's INTERFACE library).
cc_library(
    name = "transportConfiguration",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
