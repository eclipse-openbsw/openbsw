# Platform abstraction - base types and macros for portability.

cc_library(
    name = "platform",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
