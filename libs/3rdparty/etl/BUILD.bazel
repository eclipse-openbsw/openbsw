# Embedded Template Library (ETL) - header-only C++ template library.

cc_library(
    name = "etl",
    hdrs = glob(["include/**"]),
    includes = ["include"],
    deps = ["//bazel/app_config:etl_profile"],
    visibility = ["//visibility:public"],
)
